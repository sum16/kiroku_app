<% p = EventPresenter.new(event, self) %>
<%#  @events.ids.each do |a| %>
<tr>
  <td class="date"><%= p.title %></td>
  <td class="date"><%= p.application_start_time %></td>
  <td class="numeric"><%= p.application_end_time %></td>
  <td class="numeric"><%= p.min_number_of_participants %></td>
  <td class="numeric"><%= p.max_number_of_participants %></td>
  <td class="numeric"><%= p.number_of_applicants %></td>
  <td class="numeric"><%#= p.name %></td>
  <td class="actions"><%= link_to "詳細", caregiver_event_path(event.id)  %> |
                      <%= button_to "削除", caregiver_event_path(event.id), method: :delete %> 
  </td> 
</tr>
<%# end %>


