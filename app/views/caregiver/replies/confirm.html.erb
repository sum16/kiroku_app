<% @title = "問い合わせへの返信(確認)" %>
<h2><%= @title %></h2>

<div id="generic-form" class="table-wrapper">
  <%= form_with model: @reply, url: caregiver_message_reply_path(@message) do |f| %>
    <%#= render "confirming_form", f: f %>
    <div class="buttons">
      <%= f.submit "送信" %> <br>
      <%= link_to "キャンセル", "#" %>
    </div>
  <% end %>
</div>
